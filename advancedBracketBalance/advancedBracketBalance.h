#pragma once

// Function that checks whether there are brackets in a string or not
bool haveBrackets(char* string);

// Function that checks whether the brackets in a string are balanced
bool advancedBracketBalance(char* string);